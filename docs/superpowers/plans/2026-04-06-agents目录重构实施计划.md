# Agents 目录重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将角色文件统一重组到 agents/ 目录，实现模块化管理（编排师作为父目录，01-04作为子模块）

**Architecture:** 通过文件系统移动（mv）完成目录重组，无需代码变更。所有引用路径通过文本替换更新。

---

## 文件结构映射

### 需要备份的文件（移动到 bak/）

| 原始路径 | 目标路径 |
|---------|---------|
| `00-角色编排总规范.md` | `bak/00-角色编排总规范.md` |
| `01-AI内容总导演-角色职责规范.md` | `bak/01-AI内容总导演-角色职责规范.md` |
| `02-AI美术技术总监-角色职责规范.md` | `bak/02-AI美术技术总监-角色职责规范.md` |
| `03-AI镜头序列设计师-角色职责规范.md` | `bak/03-AI镜头序列设计师-角色职责规范.md` |
| `04-AI品控合规官-角色职责规范.md` | `bak/04-AI品控合规官-角色职责规范.md` |
| `05-编排师-角色职责规范.md` | `bak/05-编排师-角色职责规范.md` |

### 需要移动的文件（移动到 agents/）

| 原始路径 | 目标路径 |
|---------|---------|
| `agents/audit-agent.md` | `agents/audit/audit-agent.md` |
| `docs/execution/Phase0-品控合规官/` | `agents/orchestrator/compliance/Phase0/` |
| `docs/execution/Phase1-内容总导演/` | `agents/orchestrator/director/Phase1/` |
| `docs/execution/Phase1.5-镜头序列设计师/` | `agents/orchestrator/shot-designer/Phase1.5/` |
| `docs/execution/Phase2-美术技术总监/` | `agents/orchestrator/art-director/Phase2/` |
| `docs/execution/Phase3-审核智能体/` | `agents/audit/Phase3/` |
| `rules/Seedance2/` | `agents/orchestrator/Seedance2/` |

### 需要更新引用路径的文件

| 文件 | 搜索关键词 | 替换为 |
|------|---------|--------|
| `CLAUDE.md` | `docs/execution/` | `agents/orchestrator/{子模块}/` |
| `docs/路径规范.md` | `docs/execution/` | `agents/orchestrator/{子模块}/` |
| `docs/快速入门指南.md` | `docs/execution/` | `agents/orchestrator/{子模块}/` |
| `agents/audit/audit-agent.md` | 相对路径 | 更新为 `dimensions/`、`templates/`、`rules/`、`Phase3/` |
| `agents/orchestrator/orchestrator.md` | `docs/execution/` | 对应的新路径 |

---

## Task 1: 创建 bak 目录并备份根目录文件

**Files:**
- Create: `bak/` (directory)

- [ ] **Step 1: 创建 bak 目录**

```bash
mkdir -p bak
```

- [ ] **Step 2: 移动根目录文件到 bak/**

```bash
mv 00-角色编排总规范.md bak/
mv 01-AI内容总导演-角色职责规范.md bak/
mv 02-AI美术技术总监-角色职责规范.md bak/
mv 03-AI镜头序列设计师-角色职责规范.md bak/
mv 04-AI品控合规官-角色职责规范.md bak/
mv 05-编排师-角色职责规范.md bak/
```

- [ ] **Step 3: 验证备份**

```bash
ls bak/
# 应显示：00-角色编排总规范.md, 01-AI内容总导演-角色职责规范.md, 02-AI美术技术总监-角色职责规范.md, 03-AI镜头序列设计师-角色职责规范.md, 04-AI品控合规官-角色职责规范.md, 05-编排师-角色职责规范.md
```

- [ ] **Step 4: 提交**

```bash
git add bak/ && git commit -m "chore: backup original role files to bak/"
```

---

## Task 2: 移动 audit-agent.md 到 agents/audit/

**Files:**
- Modify: `agents/audit/audit-agent.md` (内部路径引用)

- [ ] **Step 1: 移动 audit-agent.md**

```bash
mkdir -p agents/audit
mv agents/audit-agent.md agents/audit/audit-agent.md
```

- [ ] **Step 2: 更新 audit-agent.md 内部路径**

读取 `agents/audit/audit-agent.md`，找到以下引用并更新：

| 旧引用 | 新引用 |
|--------|--------|
| `agents/audit/dimensions/` | `dimensions/` |
| `agents/audit/templates/` | `templates/` |
| `agents/audit/rules/` | `rules/` |
| `docs/execution/Phase3-审核智能体/` | `Phase3/` |

- [ ] **Step 3: 验证 audit-agent.md 内部引用**

```bash
grep -E "dimensions/|templates/|rules/|Phase3/" agents/audit/audit-agent.md
# 确保没有 `agents/audit/` 前缀
```

- [ ] **Step 4: 提交**

```bash
git add agents/audit/audit-agent.md && git commit -m "refactor: move audit-agent.md into agents/audit/ and update paths"
```

---

## Task 3: 创建 orchestrator 父目录结构

**Files:**
- Create: `agents/orchestrator/` (directory)
- Create: `agents/orchestrator/director/Phase1/` (directory)
- Create: `agents/orchestrator/art-director/Phase2/` (directory)
- Create: `agents/orchestrator/shot-designer/Phase1.5/` (directory)
- Create: `agents/orchestrator/compliance/Phase0/` (directory)
- Create: `agents/orchestrator/execution/` (directory)
- Create: `agents/orchestrator/Seedance2/` (directory)

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p agents/orchestrator/director/Phase1
mkdir -p agents/orchestrator/art-director/Phase2
mkdir -p agents/orchestrator/shot-designer/Phase1.5
mkdir -p agents/orchestrator/compliance/Phase0
mkdir -p agents/orchestrator/execution
mkdir -p agents/orchestrator/Seedance2
```

- [ ] **Step 2: 验证目录创建**

```bash
find agents/orchestrator -type d | sort
# 应显示所有新目录
```

- [ ] **Step 3: 提交**

```bash
git add agents/orchestrator/ && git commit -m "chore: create orchestrator parent directory structure"
```

---

## Task 4: 移动执行文档和规则文件

**Files:**
- Move: `docs/execution/Phase0-品控合规官/` → `agents/orchestrator/compliance/Phase0/`
- Move: `docs/execution/Phase1-内容总导演/` → `agents/orchestrator/director/Phase1/`
- Move: `docs/execution/Phase1.5-镜头序列设计师/` → `agents/orchestrator/shot-designer/Phase1.5/`
- Move: `docs/execution/Phase2-美术技术总监/` → `agents/orchestrator/art-director/Phase2/`
- Move: `docs/execution/Phase3-审核智能体/` → `agents/audit/Phase3/`
- Move: `rules/Seedance2/` → `agents/orchestrator/Seedance2/`

- [ ] **Step 1: 移动 Phase0**

```bash
mv docs/execution/Phase0-品控合规官/* agents/orchestrator/compliance/Phase0/
```

- [ ] **Step 2: 移动 Phase1**

```bash
mv docs/execution/Phase1-内容总导演/* agents/orchestrator/director/Phase1/
```

- [ ] **Step 3: 移动 Phase1.5**

```bash
mv docs/execution/Phase1.5-镜头序列设计师/* agents/orchestrator/shot-designer/Phase1.5/
```

- [ ] **Step 4: 移动 Phase2**

```bash
mv docs/execution/Phase2-美术技术总监/* agents/orchestrator/art-director/Phase2/
```

- [ ] **Step 5: 移动 Phase3**

```bash
mv docs/execution/Phase3-审核智能体/* agents/audit/Phase3/
```

- [ ] **Step 6: 移动 Seedance2**

```bash
mv rules/Seedance2/* agents/orchestrator/Seedance2/
```

- [ ] **Step 7: 验证移动结果**

```bash
find agents/orchestrator -name "*.md" | head -20
ls agents/audit/Phase3/
```

- [ ] **Step 8: 提交**

```bash
git add agents/orchestrator/ agents/audit/Phase3/ && git commit -m "refactor: move execution docs and Seedance2 rules to agents/"
```

---

## Task 5: 更新引用路径

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/路径规范.md`
- Modify: `docs/快速入门指南.md`
- Modify: `agents/audit/audit-agent.md` (如有遗漏)
- Create: `agents/orchestrator/orchestrator.md` (从 bak/ 移回的入口文件)

- [ ] **Step 1: 从 bak/ 移回 orchestrator.md 到 agents/orchestrator/**

```bash
mv bak/05-编排师-角色职责规范.md agents/orchestrator/orchestrator.md
```

- [ ] **Step 2: 更新 CLAUDE.md 中的路径引用**

使用 Edit 工具替换：
- `docs/execution/Phase0-品控合规官/` → `agents/orchestrator/compliance/Phase0/`
- `docs/execution/Phase1-内容总导演/` → `agents/orchestrator/director/Phase1/`
- `docs/execution/Phase1.5-镜头序列设计师/` → `agents/orchestrator/shot-designer/Phase1.5/`
- `docs/execution/Phase2-美术技术总监/` → `agents/orchestrator/art-director/Phase2/`
- `docs/execution/` → `agents/orchestrator/`

- [ ] **Step 3: 更新 docs/路径规范.md 中的路径引用**

使用 Edit 工具替换相同的路径映射。

- [ ] **Step 4: 更新 docs/快速入门指南.md 中的路径引用**

使用 Edit 工具替换相同的路径映射。

- [ ] **Step 5: 更新 agents/orchestrator/orchestrator.md 中的路径引用**

读取文件，找到所有 `docs/execution/` 引用并更新为新路径。

- [ ] **Step 6: 更新 agents/audit/audit-agent.md 中的 Phase3 路径**

如果 audit-agent.md 中有 `docs/execution/Phase3-审核智能体/` 引用，更新为 `Phase3/`。

- [ ] **Step 7: 验证所有引用已更新**

```bash
grep -r "docs/execution/" --include="*.md" .
grep -r "rules/Seedance2" --include="*.md" .
# 应该没有结果（或只有 bak/ 下的备份文件中有）
```

- [ ] **Step 8: 提交**

```bash
git add CLAUDE.md docs/路径规范.md docs/快速入门指南.md agents/orchestrator/orchestrator.md agents/audit/audit-agent.md && git commit -m "refactor: update all reference paths to new agent locations"
```

---

## Task 6: 验证与清理

**Files:**
- Delete: `docs/execution/` (空目录)
- Delete: `rules/Seedance2/` (空目录)

- [ ] **Step 1: 删除空目录**

```bash
rmdir docs/execution/Phase0-品控合规官 docs/execution/Phase1-内容总导演 docs/execution/Phase1.5-镜头序列设计师 docs/execution/Phase2-美术技术总监 docs/execution/Phase3-审核智能体 docs/execution/ rules/Seedance2/
```

- [ ] **Step 2: 验证目录结构**

```bash
# 验证 bak/ 目录
ls bak/

# 验证 agents/ 目录结构
find agents/ -type d | sort

# 验证关键文件存在
ls agents/orchestrator/orchestrator.md
ls agents/audit/audit-agent.md
ls agents/orchestrator/director/Phase1/
ls agents/orchestrator/art-director/Phase2/
ls agents/orchestrator/shot-designer/Phase1.5/
ls agents/orchestrator/compliance/Phase0/
ls agents/orchestrator/Seedance2/
ls agents/audit/Phase3/
```

- [ ] **Step 3: 最终验证无遗留跨目录引用**

```bash
grep -r "docs/execution/" --include="*.md" . | grep -v "^./bak/"
grep -r "rules/Seedance2" --include="*.md" . | grep -v "^./bak/"
# 应无输出
```

- [ ] **Step 4: 提交清理**

```bash
git add -A && git commit -m "chore: remove empty directories after migration"
```

---

## 验证检查清单

- [ ] bak/ 目录包含 6 个原始文件
- [ ] agents/audit/audit-agent.md 存在且内部路径已更新
- [ ] agents/orchestrator/orchestrator.md 存在
- [ ] agents/orchestrator/director/Phase1/ 包含原 Phase1 执行文档
- [ ] agents/orchestrator/art-director/Phase2/ 包含原 Phase2 执行文档
- [ ] agents/orchestrator/shot-designer/Phase1.5/ 包含原 Phase1.5 执行文档
- [ ] agents/orchestrator/compliance/Phase0/ 包含原 Phase0 执行文档
- [ ] agents/orchestrator/Seedance2/ 包含原 Seedance2 规则文件
- [ ] agents/audit/Phase3/ 包含原 Phase3 执行文档
- [ ] docs/execution/ 目录已删除
- [ ] rules/Seedance2/ 目录已删除
- [ ] CLAUDE.md 中无 docs/execution/ 引用
- [ ] docs/路径规范.md 中无 docs/execution/ 引用
- [ ] docs/快速入门指南.md 中无 docs/execution/ 引用
- [ ] 无遗留跨目录引用

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-04-06 | 初版实施计划 |
